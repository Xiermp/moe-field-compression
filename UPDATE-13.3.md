# UPDATE-13.3 — WH-CHECK: проверка компонентов по четырём подозрениям

Дата: 2026-09-08. Базовая версия: 13.1/13.2-tools (2026-09-08.3).
Повод: внешний анализ обвинил (1) отбеливание в «розовых очках» без демпфера,
(2) фит в дрейфе центроидов W0, (3) ядро Cdn в параличе и Cgu в разгоне
(5.3 → 201), (4) preact-режим в насыщении SiLU. Ниже — проверка КАЖДОГО
пункта по коду, что уже было в системе, что добавлено, и как получить
решающие числа за один прогон.

Сразу главное: **deploy_check дал ratio 1.000 на всех блоках** — путь
«модуль → артефакт» ЧИСТ (подозрение UPDATE-13.2 снято). Качество артефакта =
качество инита/фита самого по себе. И: два пункта из четырёх — неверное
чтение лога (max|d| — это РАЗНИЦА фит-vs-артефакт, а не величина тензора).

---

## Вердикт 1: «отбеленная метрика — розовые очки, нужен демпфер»

**Демпфер уже есть, и он жёстче предлагаемого.** `whbank_build.inv_sqrt`
строит обратный корень с хребтом `eps = 1e-3 · λmax` (на ОБРАТНОЙ стороне —
именно там, где он нужен). Предлагаемый рецепт «λ = 0.01 · mean(diag(Σ))»:
при вырожденной ковариации λmax ≈ 1000 · mean(diag), т.е. наш хребет
≈ 1 · mean(diag) — **в ~100 раз больше** предлагаемого. Прямой корень
(`sqrtm_psd`) имеет свой eps = 1e-6·λmax (там усиления нет, это безопасно).

**Механика «Σ^-1/2 умножает ошибки и взрывает mse» — неверна для пула.**
Отбеленный capture — это НЕ учётная уловка: минимизация
`||ΔW̃ − Ã||²`, где `ΔW̃ = Cy^{1/2} ΔW Cx^{1/2}`, тождественна минимизации
`tr((ΔW−A) Cx (ΔW−A)ᵀ Cy)` — ожидаемой ошибки ВЫХОДА на калибровочном
распределении. 99.8% capture честно означают: gu-сторона поля оставляет
0.2% output-error энергии дельт на калибровке. «Умножение на 1/λmin» бьёт
по ВЕСАМ в низкоэнергетических направлениях, но их вклад в калибровочный
mse и так взвешен тем же Σ — двойного счёта нет.

**Реальные слепые пятна отбеленной метрики** (поэтому 13.3 добавляет вторую
линзу, а не выбрасывает первую): генерализация за пределы калибровки
(низкоэнергетические направления могут ожить на других данных), нелинейность
preact (метрика линейна по выходу), capture — про дельты, а не end-to-end.

**Новое в 13.3 — честный (plain-Frobenius) capture рядом с отбеленным:**

```
gu delta energy captured at step 0 - 99.8%  (t2_k64, отбеленная метрика) | честный (Frobenius) NN.N%
```

Считается норм-трюком (`raw_capture`, без реконструкции m×n; проверен тестом
против явной реконструкции до 1e-5). Попадает в npz банка (`caps_raw`), в
`--verify`, в init-словарь и в SVD-init баннер пайплайна. Как читать:

- `raw ≈ whitened` — захват распределён по спектру устойчиво;
- `raw << whitened` — захват живёт в немногих активационных направлениях
  (выбросах). Это НЕ «банк плох» и НЕ «метрика врёт» — это измеренная
  вырожденность ковариации: сигнал о том, что калибровка узка.

Настраиваемый демпфер: `--wh-damp-frac F --wh-damp-base {lmax,mean-diag}`
(дефолт `1e-3·lmax` = текущее поведение бит-в-бит; рецепт внешнего анализа =
`0.01 · mean-diag`). Чужой damp в cov-кэше пересчитывает факторы; кастомный
демпфер меняет отбеливание → банки надо перестроить (`--force`, есть гвард).

## Вердикт 2: «центроиды W0 обучаются — заморозить»

**Подтверждено кодом.** В `train="core"` (13.1) морозятся только U*/V*
(в логе «4 U*/V* basis factors frozen»); `wgud/wdnd` (центроиды) и ядра
обучаются. Это было осознанное решение 13.1 («почти выпуклая полировка»),
не баг. `e_score_correction_bias` — буфер, фитом не трогается; gw заморожен.

Оценка риска числом: AdamW-шаг ~lr на координату → потолок дрейфа
`lr · steps = 1e-4 · 100 = 1e-2` абсолютных; масштаб центроидов на реальной
модели (из вашего же deploy_check): `max|wdnd| ≈ 1.2e-4·(256..512) ≈ 0.03`.
Worst case ~30% собственного масштаба — опасение обоснованное, но это
потолок, не факт.

**Новое в 13.3:**

1. `train="cores"` — центроиды заморожены НАМЕРТВО, обучаются только ядра
   Cgu/Cdn. Лог: `fit-train cores: 6 basis/centroid factors frozen, cores
   only train (centroids frozen)`. Дефолт НЕ менял — решит A/B (ниже).
2. Drift-строки в конце каждого polish-фита (обоих режимов):

```
    blk 0 drift wgud: max|d| 3.1e-03 (2.41% of max|p0| 1.3e-01)
```

Спор «сдвинулся W0 или нет» закрывается числом из ВАШЕГО прогона.

## Вердикт 3: «Cdn = ноль, Cgu вразнос 5 → 201, preact насыщает SiLU»

**Чтение лога неверное.** В deploy_check `max|d|` — максимум РАЗНИЦЫ между
фитом и артефактом (шум bf16-записи), а не величина тензора. `rel 0.39%` у
всех тензоров подряд — это теоретический потолок bf16 (2⁻⁸): запись ВЕРНА,
все тензоры совпадают с фитом с точностью округления. Именно поэтому
ratio 1.000.

Реальные величины ≈ `max|d| · (256..512)` (у bf16 относительная точность
0.2–0.4%): Cgu ~1.4e3…1e5, Cdn ~8e-3…1.5. Это единицы ОТБЕЛЕННОГО
пространства: C = Uwᵀ (Cy^{1/2} ΔM Cx^{1/2}) Vw, и масштаб C определяется
√ковариаций. Сравнивать Cgu с Cdn «в лоб» нельзя — разные стороны, разные
ковариации, разные единицы. Реконструкция `U·C·V` от масштаба C не зависит.

- «Cdn должен быть 1.0–5.0 как Cgu» — беспочвенно. Реальная слабость dn
  известна из ваших же логов и измеряется не амплитудой ядра, а capture:
  **dn 37–81%** против gu ~99% — вот что надо чинить (см. «Что дальше»).
- Рост Cgu с глубиной (×~50 в беленных единицах) = рост активационных
  выбросов с глубиной. Известный факт трансформеров, не «разгон».
- «Матрица с весами 201 залетает в SiLU» — подмена объекта: в SiLU залетает
  РЕАЛЬНАЯ коррекция preact = реконструкция реальных дельт экспертов
  (`U·G_mix·Vᵀ` в исходном пространстве). Настоящие эксперты имеют те же
  порядки preact — «насыщение» было бы свойством данных, а не артефакта.
- postact существует с 10.9 (флаг `--field-mode postact`), но он про
  кросс-члены смеси при top_k>1 (нулевая ошибка кросс-членов ценой ~+1
  GEMM/токен), а не про «спасение от насыщения». Переключать только как
  эксперимент.
- «Добавить weight_decay 0.01 ядрам» — уже есть: polish-профиль = adamw,
  `wd = 0.01` зашит в fit для adamw с 13.1.

## Вердикт 4: «block 0 mse 0.0017, а block 20 mse 0.297»

Абсолютные mse НЕ сравнимы между блоками: E[y²] (масштаб сигнала) растёт с
глубиной вместе с выбросами. Сравнивать надо **rel-rms = √(mse / E[y²])**.
13.3 печатает его в deploy_check:

```
  mse: фит 0.297215 (2.1% rms) | репортед 0.29878 | артефакт 0.297221
       (2.1% rms) -> ratio 1.000 OK   [E[y^2] = 208.4]
```

Если на глубоких блоках rel-rms единицы процентов — функционально артефакт
заметно лучше, чем кажется по абсолютным числам, и «взрыва на хвосте» нет.
Если десятки — реальная проблема ёмкости, тогда «Что дальше», п. 3.

И да, block 0 (0.0017) против block 1 (0.049) — это не «поломка после
блока 0»: block 0 — слой НА входных эмбеддингах, там эксперты почти
 одинаковы, дельты крошечные.

---

## Состав правок (версии 2026-09-08.4)

| Файл | Что |
|---|---|
| `whbank_build.py` | `raw_capture` (норм-трюк); `caps_raw` в npz; печать обоих capture в build/reuse/verify; `--wh-damp-frac/--wh-damp-base`; damp в cov-кэше с авто-пересчётом; гвард устаревших банков при кастомном демпфере |
| `hf_field_transform.py` | `train="cores"` (мороз U*/V* + центроидов); drift-строки для core/cores; валидация all/core/cores |
| `hf_pipeline.py` | `--fit-train` += cores; SVD-init баннер печатает честный capture (если банк содержит caps_raw) |
| `deploy_check.py` | колонка `max|t|` (величина тензора) рядом с `max|d|`; rel-rms mse + E[y²] |
| Тесты | `test_update133_whcheck.py`: 6 тестов (норм-трюк == brute force; границы; eps_abs; cores морозит центроиды и учит ядра; смоук build: при единичном отбеливании whitened == raw). Регресс 13.1: 4/4 |

Совместимость: дефолты НЕ изменены (polish = core, damp = 1e-3·λmax) —
существующие банки/кэши/фиты валидны; `fit_sig` различает `core`/`cores`,
так что переключение режима само запускает ре-фит.

## Протокол A/B: «core» vs «cores» (+ решающие числа)

Один прогон polish-фита ~20–30 мин (100 шагов/блок, fp32 ~0.6 с/шаг).

```
# прогон A — как было (центроиды обучаются):
python hf_pipeline.py ... --bank-init t2 --rank 64 --stages fit,save,verify

# прогон B — рецепт внешнего анализа (центроиды заморожены):
python hf_pipeline.py ... --bank-init t2 --rank 64 --fit-train cores --stages fit,save,verify
```

Что сравнить в логах (по каждому блоку):

1. `drift wgud / wdnd` прогона A — если центроиды реально ползут (>5–10%),
   подозрение п.2 подтверждено; если ~1% — пункт закрыт.
2. `init baseline (eval8)` и финальный `guard: mse X -> Y` — где лучше.
3. `deploy_check --blocks 0-3` и `--blocks 20-22` на ОБЕИХ сборках —
   смотреть rel-rms по блокам, а не абсолютный mse.
4. КЛЮЧЕВОЕ: `--stages ... verify` даёт KL артефакта — итог A/B по нему.

## Что дальше (приоритет)

1. **A/B core vs cores** + drift-числа → судьба центроидов решается измерением.
2. **Честный capture**: быстрая проба без пересборки —
   `python whbank_build.py --pool <pool> --src <модель> --blocks 0,10,20 --verify --variants t2`
   (пересчёт на эксперте 0); полная — перестроить банки `--force` (получите
   caps_raw на всех экспертах + в SVD-баннере). Если `raw << whitened` на
   глубоких блоках — пробуйте `--wh-damp-frac 0.01 --wh-damp-base mean-diag
   --force` (рецепт внешнего анализа) и сравните KL.
3. **dn-сторона — главный кандидат по ёмкости** (capture 37–81%): проба
   топлива спайков из 13.2 — `whbank_build --variants spk --spk-rank 64` на
   глубоких блоках; `awq@1% ≥ 25%` → UPDATE-14 (разреженные всплески).
4. Если rel-rms на глубоких блоках окажется мал, а KL всё ещё плохой —
   подозрение возвращается на роутер/смесь (probe A был плоским по позициям):
   `router_audit.py` и `--fit-router` как следующий рычаг.
