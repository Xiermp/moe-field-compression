# UPDATE-13.5 - DU-BANK: пер-экспертные дельты на ВТОРОЙ матрице (u_mode)

Дата: 2026-09-10. Базовый код: 13.4-audit (hf_field_transform/hf_pipeline/
whbank_build 2026-09-08.4). Новые файлы: **test_update135_du.py**. Изменены:
hf_field_transform.py, hf_pipeline.py, modeling_field_template.py,
modeling_field_HYV3_ready.py, probe_capacity.py, init_audit.py.

## 1. Мотивация - где именно сидел потолок (цикл внешнего ревью fig11-fig14)

Стенд ревью: 4 домена, E=16, top-4, 600 шагов Adam, та же метрика relMSE.

1. **fig11** (пересборка MatrixFieldMoE с фиксами F1-F5): поле против базы
   +31% (оригинал +3%), разрыв к классике сжат до +40% (0.0422 vs 0.0332).
   Фиксы не изменили потолок - значит, дело в параметризации.
2. **fig13** (перенос ТОЧКИ коннектора/модуляции на U-сторону БЕЗ
   пер-экспертной параметрики): 0.0433-0.0468 - ВСЁ хуже базовых 0.0422.
   Перенос коннектора сам по себе пуст.
3. **fig12** (пер-экспертные ВЫХОДНЫЕ дельты на второй матрице): ПРОРЫВ -
   rank4 0.0396, full **0.0315 < классики 0.0332** (закрыто 119% разрыва
   к классике) при 2.2x меньшей параметрике.
4. **fig14** (плотность комбинаций): размер комбинационного словаря
   (coverage) монотонно предсказывает качество: Spearman(coverage, relMSE)
   = **-0.94**; лестница A2 0.36 < A2L 0.39 < U4 0.48 < UFull 0.67
   (классика 0.81). **Novelty = РОВНО 0** у всех координатных (V-сторона)
   вариантов - новые ВЫХОДНЫЕ направления могут появиться только через
   вторую матрицу. Interaction: классика 0.0000 (линейный комбайнер),
   поле 0.29-0.59 (кросс-члены сохраняются).

Вывод: потолок поля - не смесь (её банк 2 уже расширяет), а ВЫХОДНОЙ
словарь: (h@Vdn) всегда проецируется на один и тот же span(Udn). 13.5
добавляет пер-экспертную дельту именно туда, с LoRA-стартом.

## 2. Что добавлено

**u_mode** (config.field.u_mode, флаги --u-mode/--u-rank): пер-экспертные
дельты на down-факторе. Для выбранного эксперта e:

    du_e = duAdn[e] @ duBdn[e]^T          # rank1 (a=1) / rank4 (a=u_rank)
    du_e = duEdn[e]                       # full: (r, d) целиком

Терм добавляется к выходу ПОСЛЕ нелинейности, top-k-смесь (в postact -
внутри per-expert цикла, в preact - отдельным циклом по top-k; hv = h@Vdn
общий, h не зависит от эксперта):

    y += sum_j z_j * ((h @ Vdn) @ du_ej)        # [T, r] -> [T, d]

- **LoRA-старт**: duBdn/duEdn нулевые -> шаг 0 бит-в-бит равен u_mode none
  (проверено t1 на обеих сторонах, обеих композициях).
- **Имена du*** (duAdn/duBdn/duEdn) выбраны НАМЕРЕННО не в паттерн U*/V*:
  (а) muon-сплит по имени не забирает du в Newton-Schulz (du* остаются в
  Adam); (б) freeze в --fit-train core/cores не морозит du - новая ёмкость
  обязана тренироваться даже в полировке.
- duAdn стартует randn*0.02 (тот же масштаб, что guard-фикс B1 у U,V),
  поэтому градиент duBdn жив с шага 0; duAdn оживает со второго шага
  (классика A@B^T) - это нормально, стенд fig12 это подтверждал.
- **Ключи в config.field**: u_mode/u_rank. Старые артефакты (ключа нет) =
  none, новый шаблон читает их через fi.get - поведение бит-в-бит.
- **Совместимость**: чекпоинт без du-ключей грузится в du-модуль без
  изменений поведения (du = 0); обратный роллбэк - пересборка без
  --u-mode. bank_apply перезаписывает шафлы с сохранением ВСЕХ ключей и
  правит config точечно - du-тензоры переживают подмену банка.

## 3. Бюджеты (на блок; на модель умножать на n_layers)

| Режим | Параметры/блок | MAC/токен (top_k=4) | fit-каталог |
|---|---|---|---|
| none (13.4) | 0 | 0 | fit_r\<rank\>[b2][t2] |
| rank1 | +E*(r+d) | ~top_k*(r + d) | fit_r\<rank\>du1 |
| rank4 (a=4) | +E*(r+d)*4 | ~top_k*(r*4 + 4*d) | fit_r\<rank\>du4 |
| full | +E*r*d | ~top_k*r*d | fit_r\<rank\>duF |

Пример r=64, d=4096, E=64: rank1 +0.27M, rank4 +1.06M, full +16.8M
параметров на блок. Для 23-блочной модели в fp16: rank4 ~ +49 МБ,
full ~ +772 МБ к полю (поле 13.4 ~ 107 МБ). full брать осознанно:
начинать с rank4 (на стенде он забрал большую часть прорыва: 0.0396
против 0.0422 у базы, full дожимал до 0.0315).

fit_sig теперь несёт u_mode/u_rank -> смена режима перезапускает фит в
СВОЁМ каталоге, старые фиты не трогаются.

## 4. probe_capacity: арм 'du'

    python probe_capacity.py --root <pool> --block 20 --arm du --du-rank 4

Тёплый старт из shipped-фита, свежие duA (randn*0.02) + duB=0, фит как у
bank2-арма. Чтение результата (сравнивать с cont И bank2 на том же блоке):

| Наблюдение | Вердикт |
|---|---|
| du << cont | потолок - выходной словарь: включать --u-mode rank4/full |
| du ~= cont, bank2 << cont | потолок в смеси -> bank2, du не спасает |
| du и bank2 оба ~= cont | остаток - недосходимость/ранг базы (см. 13.4) |

## 5. init_audit: синхронизация с du

--u-mode/--u-rank (по умолчанию читаются из fit_meta.json - там теперь
лежит весь fit_sig): аудит строит РОВНО тот модуль, что и артефакт -
du-параметры не отваливаются молча от фитов с u_mode != none.

## 6. Тесты

test_update135_du.py (без transformers, шаблон рендерится на стабы):
t1 LoRA-старт бит-в-бит (3 режима x 2 композиции, фит+шаблон); t2 градиенты
шага 0 (duBdn/duEdn живы, duAdn спит ровно шаг); t3 train="cores": du
тренируется (mse 4.6e-3 -> 1.7e-3), базис заморожен; t4 roundtrip
fit->init; t5 шаблон==фит (6 конфигураций, max|d|=0); t6 ключи state_dict
по режимам; t7 accounting (+E*(r+d)*a*2 / +E*r*d*2 байт); t8 muon-сплит.
**ВСЕ ПРОЙДЕНЫ.** Регрессии: 13.1 - 4/4, 13.2 - 3/3, 13.3 - 6/6,
13.4 - 5/5.

## 7. План прогона

1. **Зонд** (не пересобирая модель): `probe_capacity --arm du` на 2-3
   худших блоках (blk20/22/19 по логу 13.3) рядом с --arm cont/bank2.
2. Если du платит: полный rerun с `--bank-init t2 --field-mode postact
   --fit-train cores --u-mode rank4 --u-rank 4` (postact - под вердикты
   13.4 о кросс-членах; du поверх postact ложится в per-expert цикл без
   новых проходов по экспертам).
3. A/B: тот же прогон без --u-mode (каталоги фит не делят) - KL/ppl в
   стадии 7 решает, остаётся ли du в дефолте; затем при желании лестница
   --u-rank 8/16 или --u-mode full.
4. Роллбэк в любой момент: пересборка без --u-mode; старые артефакты на
   новом шаблоне работают как есть.

## 8. 13.5.1 - фиксы логирования по итогам первого реального прогона

Первый прод-прогон рецепта (NanoColibri-Instruct Q8_0, rank 64, postact,
u-mode rank4) прошёл числово чисто (fit 10.9-49.3% ниже step-0 на всех
завершённых блоках, duBdn жив на всех блоках), но лог вскрыл 4 дефекта
печати/таймеров. Все фиксы - только печать и источник времени, ноль
изменений в числодробилке:

1. `print_profile` не показывал u_mode/u_rank - прогон с rank4 был
   неотличим в шапке от none. Теперь в profile-строке есть
   `u_mode=rank4/r4` (полный - `u_mode=full`).
2. drift нового нулевого параметра (duBdn) печатал астрономический
   процент от max(s, 1e-12) - теперь `(new param, max|p0| = 0)` и
   абсолютный max|d|.
3. Сводка stage 4 писала "0 rebuilt, 23 reused", хотя 23 SVD-init
   файлы БЫЛИ пересобраны за те же 376.6s (stale-версия алгоритма
   init) - теперь счёт SVD-init rebuilt входит в строку.
4. hf_stream: время загрузки блока на time.time() однажды ушло в минус
   "(block -0.3 s)" (NTP-подстройка часов) - переведено на
   perf_counter (монотонный).
5. Формулировка guard-строки: "% below step 0" вместо "below the init
   baseline" (процент считается от step-0 minibatch mse, eval8-бейзлайн
   печатается отдельной строкой) + fit-train строка теперь честно
   пишет "+ du-bank (new capacity)".

Тесты после фиксов: 13.5 du - все, 13.1 4/4, 13.2 3/3, 13.3 6/6,
13.4 5/5, dtype - все. Старые фиты совместимы: числовая часть не
трогалась, кэши/артефакты валидны, пересборка не нужна.
