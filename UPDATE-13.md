# UPDATE-13 — build 13 (2026-09-08): whbanks — отбелённые банки дельт, оба варианта рядом (Tucker-2 с плотным ядром + wh-SVD)

Релиз по итогам аналитической фазы после 12: фит joint r=128 на реальной
модели дал capture **0.3%** (gu/dn стабильно по всем 23 блокам), и разбор
показал, что «дельты — шум» было **ошибкой линзы, не факта**: шумовой пол
для общего базиса + ДИАГОНАЛИ = 128/(m·n) ≈ 0.001–0.01%, наблюдаемые 0.3% —
на два порядка выше пола. Сигнал есть, но живёт в **cross-членах** общей
подпространства (u_iᵀ Δ v_j, i≠j), невидимых диагональю.

Ответ — две сборки, которые теперь живут рядом и проверяются одним
инструментом:

| вариант | идея | шаг-0 capture | контейнер |
|---|---|---|---|
| **t2_k\*** (Tucker-2) | общий отбелённый базис + ПЛОТНОЕ ядро k×k на эксперта: ΔW_e = U·G_e·Vᵀ, G_e = Uᵀ ΔW_e V (LS-оптимум при фиксированном базисе — забирает ВСЕ cross-коэффициенты) | ожидание 15–65% при затухающем спектре (гейт 45%) | **да** — единственная правка payload: вектор C → матрица (r, r) |
| **whsvd_r\*** (wh-SVD, ×3.0) | per-expert отбелённый SVD, ранг 16, масштаб LS-α×3.0 — «пиковый» вариант | потолок качества (на игрушке: KL 0.012 бит/ток, Δppl +1.2% — бьёт ранг 128) | **нет** — per-expert базис не выражается в общий базис поля; только диагностика/потолок |
| dia_d128 | легаси: общий базис + диагональ (старый bank1) | ~0.3% (якорь паритета с логами joint-v1) | да (текущий формат) |

Ключевой сдвиг линзы: capture считается в **отбелённой метрике**
(Mw = Cy^{1/2}·Δ·Cx^{1/2}), т.е. функционально — взвешенно реальными
активациями из кэша pair pool. Пер-экспертный жадный сбор шума
квантифицируется Marchenko-Pastur edge (`(r/p)·(1+√(p/N))²`) — именно он
объяснял 2.4% старого bank2 как шум; авто-стоп каскада привязан к 1.3×MP.

## Что нового

### 1. `whbank_build.py` — сборка и проверка ОБЕИХ семей (новый файл)

    # отчёт по уже собранным банкам
    python whbank_build.py --pool <cache_dir> --report-only
    # сборка на блоке 0 (демо всей цепочки, ~минуты): cov → базис → банки → отчёт
    python whbank_build.py --pool <cache_dir> --src <модель> --blocks 0 \
        --variants t2,whsvd,dia --k 16,24,32,48,64 --r 16 --scale 3.0
    # полная сборка + проверка файлов пересчётом на свежих дельтах
    python whbank_build.py --pool <cache_dir> --src <модель> --blocks 0-22 --verify

  - **дельты — то же определение, что в expert_basis_init** (W_e − центроида,
    стриминг по одному эксперту): capture-числа сравнимы с логами joint-v1;
  - **ковариации отбеливания — из кэша pair pool**, ничего не пересобираем:
    gu: Cx = E[xxᵀ] (вход блока, точно), Cy = центроидная проекция preact;
    dn: Cx = E[h hᵀ] от центроидного gu (прокси), Cy = E[yyᵀ] (прокси);
  - факторы (C^{1/2}, C^{−1/2}) считаются **один раз** и кэшируются в
    `<out>/cov_blk{i}.npz` (eigh — самая дорогая часть);
  - банки: `<out>/t2_k{K}/blk{i}_{gu|dn}.npz` (U, V, cores, caps),
    `<out>/whsvd_r{R}/...` (A, B, scales, caps_e), `<out>/dia_d128/...`;
  - **resume**: существующие npz не пересобираются (`--force` сбрасывает);
  - память O(кусков ковараций + один эксперт) — тот же стриминговый режим,
    что в пайплайне (BlockStreamRunner или полный лоад `--local-path`);
  - REPORT-таблица: вариант × (gu, dn) capture, байт/эксперт, MP-шум, GATE.

### 2. hf_pipeline.py — флаг `--bank-init {svd,t2}` (по умолчанию svd = бит-в-бит 12)

    python hf_pipeline.py <ваши модельные флаги> --bank-init t2

Что делает:
  - **stage-4 строит Tucker-2-init вместо слепого диагонального**: cov-факторы
    и банки t2_k{rank} создаются/переиспользуются автоматически (ensure_t2_init
    внутри with_block — блок уже материализован); init_svd_blk{i}.pt несёт
    svd_ver = `whrank-t2-v1` + capture в отбелённой метрике (строка
    "SVD init (..., whitened Tucker-2)" в логе);
  - **payload вектор→матрица**: C (n_exp, r) → плотное ядро (n_exp, r, r);
  - **отдельный fit-каталог** `fit_r<rank>t2` — кэши/фиты/артефакты
    --bank-init svd не трогаются и не перепроверяются;
  - cfg.field получает `core: "dense"`, accounting считает ядро честно;
  - дефолт `--bank-init svd` — прежнее поведение **бит-в-бит** (гвард
    `_compat_check` обновлён: пакет без whbank_build.py не смешивается).

### 3. Рантайм — плотное ядро (modeling_field_template.py / HYV3_ready)

  - `config.field.core == "dense"` → C (n_exp, r, r); preact: смесь ядер
    G_mix = Σ_e z_e G_e (два батч-эйнсама, без per-expert цикла, T·r² на
    токен); postact: ядро выбранного эксперта;
  - банк 2 остаётся диагональным и в dense-режиме (ядро уже несёт
    cross-члены общего базиса);
  - артефакты **без ключа core** работают как раньше (дефолт "diag");

### 4. `bank_apply.py` — мгновенная замена банка в ГОТОВОМ артефакте (без рефита)

    python bank_apply.py --artifact <папка артефакта> \
        --whbank <fit_r...t2/whbank | pool/whbank> --variant t2_k32        # сухой прогон
    python bank_apply.py ... --variant t2_k32 --apply                      # записать

  - подменяет U/V/C на банки из whbank, обновляет config.field (rank/core) и
    field_meta.json; бэкбон, роутер, центроиды НЕ трогаются; перезаписываются
    только шафлы, где лежат тензоры поля;
  - **только t2_k\*** варианты: whsvd (per-expert базис) в общий контейнер не
    выражается — это потолок качества и диагностика;
  - это step-0 замена (capture банка без фита); полный путь замены с
    дожимом фитa — `--bank-init t2` + рефит, bank_apply — мгновенная проверка.

### 5. `whrank.py` — библиотека ранговой системы (каскад T2/DIA/GR любой глубины)

  - стадии: T2(d, k) плотное ядро / T2(d, diag=True) легаси / GR(r) per-expert
    жадный на остатке; авто-стоп: для GR — 1.3×MP-edge, для T2 — min_capture;
  - самотест: `python whrank.py` (кросс-терм сигнал + per-expert шум:
    DIA 3.9% → стоп; T2 16×16 30.8%; T2 32×32 31.9%; каскад T2→GR 9.3% → MP-стоп);
  - подключение реальных данных — через whbank_build (пункты 1–2).

## Как читать результат (чек-лист проверки)

1. `whbank_build --blocks 0 --variants t2,whsvd,dia` → REPORT блока 0:
   - `dia_d128` должен дать ~0.3–4% — якорь паритета с joint-v1 (если нет —
     линза/кэш разъехались, дальше не идти);
   - `t2_k*`: если gu/dn ≥ 45% уже при k=24–32 — замена оправдана (GATE PASS);
     если ~6–15% — спектр не концентрирован, поднимать k (48/64) или признать
     per-expert структуру (тогда контейнерный путь — это GR-стадия, UPDATE-14);
   - `whsvd_r16` vs `t2_k*`: зазор покажет, сколько энергии лежит ВНЕ общего
     базиса (потолок Tucker-2 = capture_proj, потолок wh-SVD = per-expert SVD).
2. `whbank_build --blocks 0-22 --verify` → все verify-строки OK (файлы ==
   пересчёту), REPORT по всем блокам, GATE по минимуму столбца.
3. Замена (любая):
   - быстрый путь: `bank_apply --variant t2_k<k> --apply` → та же
     iron/eval-команда на артефакте (кэши валидны — они не зависят от ранга);
   - полный путь: `hf_pipeline --bank-init t2` (stage-4 → фит → артефакт в
     `field_<tag>_r<rank>`), в логе ждать строку
     `SVD init (whrank-t2-v1, whitened Tucker-2): ... capture`.

## Откат и совместимость

  - `--bank-init svd` (дефолт) — бит-в-бит поведение UPDATE-12: старые
    fit-каталоги, init-файлы и артефакты валидны, ничего не пересчитывается;
  - `fit_r<rank>t2` — отдельный каталог: t2-эксперименты не касаются старых;
  - bank_apply меняет только папку артефакта (держите копию папки — или
    пересоберите пайплайном); старые артефакты без ключа `core` грузятся
    как раньше;
  - `_compat_check` требует whbank_build.py в пакете — частичное обновление
    папки будет поймано одной понятной ошибкой.

## Технические детали (почему это работает)

  - G_e = Uᵀ ΔW_e V — замкнутая форма, LS-оптимум при фиксированном базисе:
    диагональ берёт только k совпадающих пар (i,i), плотное ядро — все k²
    пар; при k=128 разница = 128² против 128 коэффициентов (вот откуда
    0.3% против энергии подпространства);
  - HOSVD-init общего базиса: скаттеры SL=Σ MMᵀ, SR=Σ MᵀM по экспертам,
    топ-d собственных направлений каждого — общий U и V (vstack-SVD давал
    бы базис в R^{E·m} — не пространство эксперта);
  - свип k бесплатен: один базис при K=max(k), ядра режутся C[:k,:k]
    (норм-трюк: ||M − U G Vᵀ||² = ||M||² − ||G||² на ортонормальном базисе);
  - регуляризация отбеливания: Σ+εI, eps_frac=1e-3 от λmax (страхование
    мелких направлений от усиления шума); факторы кэшируются f32;
  - прокси-честность: dn-вход отбеливается центроидной проекцией silu(g)·u,
    dn-выход — выходом блока; это прокси, а не точные per-expert активации,
    обе метрики согласованы между сборкой и verify (пересчёт из файлов).

## Файлы релиза

  - новые: `whbank_build.py`, `bank_apply.py`, `whrank.py`, `UPDATE-13.md`;
  - изменены: `hf_pipeline.py` (--bank-init, fit_r<rank>t2, stage-4 ветка,
    capture-строка, accounting), `hf_field_transform.py` (core в
    FieldSparseMoe/forward, apply_core, accounting, write_field_artifact,
    FIELD_ENGINE_VER), `modeling_field_template.py` + `modeling_field_HYV3_ready.py`
    (core в рантайме);
  - тесты: `scripts/test_update13_whbank.py` (40+ проверок: регресс диаг.
    форварда бит-в-бит, dense==ручная математика в обоих режимах,
    diag ⊂ dense, градиенты, Muon-сплит, accounting, cov-формулы,
    сборка/verify/иниц-трансфер, рантайм на моках, bank_apply на мини-артефакте,
    py_compile). Регрессы 12/11/11.2 — без изменений, зелёные.
