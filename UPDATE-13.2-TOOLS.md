# UPDATE-13.2 — tools (2026-09-08): два измерительных инструмента + разбор трёх предложений по «резкости»

Контекст: после 13.1 (фикс off-by-one + polish-профиль) обсуждались три
способа «вернуть экспертам индивидуальную резкость» на общем базисе.
Решение по каждому — ниже; оба инструмента отвечают на вопрос «где сейчас
ошибка» измерением, а не мнением.

## 1. `deploy_check.py` — на каком слое живёт ошибка (НОВЫЙ ФАЙЛ)

Сравнивает на ОДНИХ И ТЕХ ЖЕ парах кэша (`pairs_blk{i}.pt`) два мира:

- `mse_fit` — фит-модуль с параметрами из `fit_blk{i}.pt` (то, что решил фит;
  после 13.1 это может быть и pristine-инит);
- `mse_art` — те же пары через тензоры, РЕАЛЬНО записанные в артефакт
  (safetensors-шафлы, bf16 — только они и читаются, полная модель не грузится).

плюс таблица верности тензоров (shape / max|Δ| / rel err): bf16-округление
даёт ~0.4% на элемент — всё, что сильно больше, это манглинг записи
(масштаб, транспон, чужой тензор).

    python deploy_check.py --pool results/cache_<...> \
        --fit-dir results/cache_<...>/fit_r64t2 \
        --artifact field_<tag>_r64 --blocks 0-3

Как читать:
- тензоры OK и `mse_art <= 1.15 × mse_fit` → запись и функциональность верны;
  качество артефакта = качество инита/фита самого по себе → разговор про
  вместимость (ранг/спайки) уместен;
- `mse_art >> mse_fit` при тензорах OK → расхождение математики рантайма
  (шаблон modeling_field) с фит-математикой — следующий подозреваемый;
- тензор FAIL → найден конкретный баг записи.
Код выхода: 0 = OK, 1 = расхождения.

Тест: `test_update132_tools.py` — верный мини-артефакт проходит, искалеченный
Cgu (×1.5) ловится таблицей (rel 50.6% → FAIL, exit 1).

## 2. `whbank_build.py --variants spk` — проба топлива «всплесков»

Один стриминг-проход поверх готового `t2_k{rank}` (строится при отсутствии),
ничего не пишет в контейнер. Для каждого эксперта: остаток
`R = ΔW_e − U G_e Vᵀ`, функциональный вес элемента `w_ij = Cy_ii·Cx_jj`
(диагональное приближение отбеливания — AWQ-стиль навигации), затем:

- **awq-выбор** — топ-f элементов по `|R|·√w` (функционально-оптимальный
  кандидат на деплой);
- **raw-выбор** — топ-f по `|R|` (SpQR-стиль, чистая амплитуда);
- capture = доля взвешенной энергии остатка в выборе;
- ориентиры: случайная поддержка = f; топ-f гауссовского шума ≈ **14.4%**
  при f=1% (8.5% при 0.5%, 2.3% при 0.1%);
- хранение: f·m·n·6 байт/эксперт (индекс 4B + fp16 2B);
- разброс `||G_e||` по экспертам — прямое измерение «размытия громкости»:
  если амплитуды экспертов уже различаются в разы, «усреднение» — миф;
- доля остатка после t2_k64 от энергии дельты — то, что спайки вообще могут
  догонять.

**ВЕРДИКТ: топливо ЕСТЬ, если awq@1% ≥ 25%** (в ~1.7 раза выше
гауссовского ориентира и на порядок выше случайного). Тогда UPDATE-14
строит sparse-путь в контейнер; если ~14% (гаусс) — остаток после T2
элементарно шумовой, спайки не помогут, весь вопрос закрывается.

    python whbank_build.py --pool results/cache_<...> --src <модель> \
        --blocks 0-2 --variants spk --k 64

Замечание о линзах: в РАНГОВОЙ линзе остаток после T2 уже показывал
MP-шум (GR-стадия собирала ~2–3% ≈ MP edge). Спайк-линза — элементная,
она проверяется впервые; если и здесь гаусс — остаток честно шумовой
в обеих линзах.

## 3. Разбор трёх предложений (почему реализовано только измерение)

**№1, персональный гейн α_e (W_e = W₀ + α_e·U G_e Vᵀ) — отклонён как no-op.**
При плотном свободном ядре скаляр поглощается: α_e·U G_e Vᵀ = U (α_e G_e) Vᵀ.
В LS-оптимуме это переобозначение с нулевой вместимостью — «ручки
контрастности» нет, есть ядро, которое уже несёт пер-экспертную амплитуду.
Гейн имел бы смысл только при ЗАЖАТЫХ ядрах (диагональ с нормировкой) — у нас
наоборот, плотные. Сам факт «громкости» измеряется: разброс ||G_e|| в spk и
capture-вариации по блокам в логах (37–81% dn).

**№2, diversity-лосс (MSE − λ·Σ‖G_i−G_j‖²) — отклонён: посылка перевёрнута
и лосс дегенеративен.** В этом пайплайне ядра НЕ регуляризируются к общему:
T2-ядро — замкнутая форма по своей дельте; в фитe градиент ядра e течёт
только от токенов, смаршрутизированных в e. Никто не тянет их к среднему,
«лени» оптимизатора нет. Размытие, которое реально существует, живёт в
общем базисе: компоненты дельт вне span(U,V) теряются для ВСЕХ экспертов —
это измеренный 1−capture, вопрос вместимости, а не «схожести ядер».
Формально лосс неограничен снизу (без бюджетной нормы) и прямо борется
с data-термом → строго хуже mse на тех же данных.

**№3, sparse-всплески (W_e = W₀ + S_e + U G_e Vᵀ) — единственный
содержательный, но сначала проба топлива.** Прецедент (AWQ/SpQR) есть,
контейнерная совместимость достижима (аддитивная per-expert sparse),
но runtime-путь (пер-токенный sparse add) — отдельный UPDATE-14. Проба
`spk` решает за один проход, есть ли что спасать: если топ-1% элементов
функционально-взвешенного остатка несёт ≥25% его энергии — строим;
если ~14% (гауссовский уровень) — остаток не имеет «острых пиков», и
весь эффект «резкости» уже исчерпан общим базисом + ядрами.

## Порядок действий

1. **deploy_check** по 3–4 блокам → убедиться, что деплой верен (после
   13.1 артефакт должен быть = фит/p-init; если FAIL — найден баг 13.2).
2. **spk-проба** по 2–3 блокам → вердикт по топливу спайков.
3. Прислать оба вывода (и строки `init baseline (eval8)` / guard / KL нового
   прогона) — по ним выбирается следующий шаг: UPDATE-14 (спайки) или
   почка записи/рантайма, или подъём ранга/ядрa.

## Совместимость

- оба инструмента только читают/строят npz-отчёты, контейнер и кэши не
  трогают; поведение 13.1 бит-в-бит;
- тесты: `test_update132_tools.py` (3), регресс 13.1 — 4/4 зелёный.
